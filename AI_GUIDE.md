# 🤖 AI GUIDE — правила для ИИ ассистентов (обновлено для v0.6.0+)

## 🎯 Контекст проекта
FinCalc.TJ — учебный проект с **компонентной архитектурой** и **Business SaaS Minimalism дизайном**.  
Требования к коду — модульность, минимализм, переиспользуемость.

---

## 🧩 Компонентная архитектура (v0.6.0+)
Проект использует **модульную компонентную архитектуру**:
- `modules/ui/components/` - все UI компоненты
- `ComponentLoader` - динамическая загрузка компонентов
- Каждый компонент независим (HTML + JS)
- Коммуникация через события (CustomEvent)

## 🎨 Дизайн-принципы (Business SaaS Minimalism)
- **Чистый, профессиональный интерфейс**
- **Цветовая схема**: Primary: `#2563eb`, нейтральные фоны
- **Типографика**: Inter font family, четкая иерархия
- **Анимации**: Минимальные, функциональные (без 3D-эффектов)
- **Адаптивность**: Mobile-first подход

---

## 📌 Стандарты разработки
- Чистый JavaScript (ES6+)
- UI на русском, переменные на английском
- Mobile-first дизайн
- Логика отделяется от DOM
- CSS: модульный, с переменными в `:root`

---

## 🚫 Нельзя без разрешения
- добавлять React / Vue / Angular
- переходить на TypeScript
- менять финансовые формулы
- вводить ежегодный налог
- добавлять бэкенд
- возвращать сложные 3D-анимации и градиенты
- нарушать принципы минимализма
- **нарушать границы компонентов**

---

## ✅ Поощряется и разрешено
### Для архитектуры:
- Создание новых компонентов в `modules/ui/components/`
- Улучшение ComponentLoader
- Оптимизация коммуникации между компонентами
- Добавление тестов для компонентов

### Для дизайна:
- Улучшение accessibility
- Оптимизация производительности (Lighthouse)
- Упрощение CSS
- Улучшение мобильного UX
- Добавление семантической разметки

---

## 🧱 Структура компонента (стандарт)
```
modules/ui/components/[component-name]/
├── [component-name].html    # Разметка компонента
└── [component-name].js      # Логика компонента
```

### Пример структуры JS компонента:
```javascript
// modules/ui/components/[feature]/[feature].js
console.log('📦 Компонент загружается...');

export class FeatureComponent {
    constructor() {
        this.init();
    }

    init() {
        this.bindEvents();
    }

    bindEvents() {
        // Логика компонента
    }
}

// Автоматическая инициализация
window.featureComponent = new FeatureComponent();
```

---

## 🔄 Коммуникация между компонентами
### Через события:
```javascript
// Отправка события
const event = new CustomEvent('componentAction', {
    detail: { data: 'value' }
});
document.dispatchEvent(event);

// Получение события
document.addEventListener('otherComponentEvent', (e) => {
    console.log('Получено:', e.detail);
});
```

---

## 📋 Формат запроса к ИИ (обновлено)
> **Component:** какой компонент изменяем (или "new" для нового)  
> **Design:** должен соответствовать Business SaaS Minimalism  
> **Task:** кратко формулируешь задачу  
> **Input:** данные/события на входе  
> **Output:** ожидаемый результат (HTML, JS, события)  
> **Constraints:** ограничения и требования

### Пример запроса:
```
Component: calculator-form
Design: Business SaaS Minimalism (primary: #2563eb)
Task: добавить валидацию минимальной суммы депозита
Input: поле initialDeposit, минимальная сумма 1000
Output: валидация при изменении, показ ошибки в стиле дизайна
Constraints: использовать события для оповещения других компонентов
```

---

## 💡 Ответы ИИ должны содержать
1. **Код компонента** (HTML и JS)
2. **Объяснение** как это вписывается в архитектуру
3. **События** которые генерирует/слушает компонент
4. **Инструкции по интеграции** через ComponentLoader
5. **Тестирование** (использовать debug.html)
6. **Совместимость с дизайном** Business SaaS Minimalism

---

## 🧪 Тестирование и отладка
1. **debug.html** - для тестирования загрузки компонентов
2. **test-components.html** - для изолированного тестирования
3. **Консоль браузера** - диагностика ComponentLoader
4. **Lighthouse** - проверка производительности

---

## 🎯 Фокус на 2026
Приоритеты для ИИ-помощи:
1. **Стабилизация компонентной архитектуры** (v0.6.x)
2. **Business SaaS Minimalism оптимизация** (v0.7.0)
3. **PWA-функциональность** (v0.8.0)
4. **Образовательные компоненты** (v1.1.0)

---

## 🆘 Если что-то сломалось
1. Проверь `debug.html` - загружаются ли компоненты?
2. Проверь консоль - ошибки в ComponentLoader?
3. Откати изменения компонента и проверь по частям
4. Используй `test-components.html` для изолированного тестирования

---

## 📚 Дополнительные правила
1. **Не трогай `script.js` без понимания архитектуры** - это менеджер компонентов
2. **Изменяй стили осторожно** - следуй существующей системе CSS переменных
3. **Документируй события** которые генерирует твой компонент
4. **Сохраняй обратную совместимость** при изменении существующих компонентов

---

## 🎯 Конечная цель
Создать библиотеку переиспользуемых, минималистичных компонентов
для финансовых калькуляторов, соответствующих принципам
Business SaaS Minimalism и современной веб-архитектуре.
`