## 1️⃣ **ARCHITECTURE.md** - обновляем статус с учетом компонентной архитектуры

```markdown
# 📐 FinCalc.TJ — Architecture Overview

**Version:** v0.6.0+ (Component-Based Architecture)  
**Status:** Stable, Modular, Scalable

---

## 🎯 Цели архитектуры (обновлено)

Архитектура проекта направлена на:

* простоту поддержки и модульность
* прозрачность логики расчётов
* удобство масштабирования через компоненты
* **чистый, минималистичный UI в стиле Business SaaS**
* AI-friendly структуру (компоненты с четкими границами)

Ключевые принципы:

> **бизнес-логика отделена от UI и DOM**  
> **UI разбит на независимые компоненты**  
> **Компоненты загружаются динамически через ComponentLoader**

---

## 🧱 Компонентная архитектура (v0.6.0+)

### 🏗️ Структура приложения
```
index.html (оркестрационный слой, ~100 строк)
├── Header Component (modules/ui/components/header/)
├── Calculator Form Component (modules/ui/components/calculator-form/)
├── Export Panel Component (modules/ui/components/export-panel/)
└── Table & Chart Component (modules/ui/components/table-chart/)
```

### 🔧 Система загрузки компонентов
```javascript
// ComponentLoader - динамическая загрузка компонентов
class ComponentLoader {
  load(componentName, targetSelector) {
    // 1. Загружает HTML компонента
    // 2. Вставляет в DOM
    // 3. Загружает JS компонента
    // 4. Инициализирует компонент
  }
}
```

### 🔄 Взаимодействие компонентов
- **События (CustomEvent)**: Компоненты общаются через события
- **Единый стейт**: Глобальные переменные в script.js
- **Декомпозиция**: Каждый компонент отвечает за свою зону ответственности

---

## 🎨 UI-архитектура

### 🧱 Компонентный подход
1. **Header**: Навигация + Hero секция
2. **Calculator Form**: Форма ввода + Банковский выбор + Результаты
3. **Export Panel**: Кнопки экспорта + Шаринг
4. **Table & Chart**: Таблица расчетов + Визуализация

### 🎨 Дизайн-принципы
1. **Минимализм**: Убраны все ненужные элементы
2. **Компонентность**: Каждый блок - независимый компонент
3. **Адаптивность**: Mobile-first подход
4. **Доступность**: Улучшенная читаемость и навигация

### 🧱 CSS-архитектура
- **CSS переменные**: Единая система в :root
- **Компонентные стили**: Каждый компонент имеет свои стили
- **Упрощенные анимации**: Легкие переходы вместо 3D-эффектов

---

## 🧱 Общая структура проекта (обновлено)

```txt
/
├── index.html          # Оркестрационный слой (контейнеры для компонентов)
├── style.css           # Глобальные стили (Business SaaS Minimalism)
├── script.js           # Менеджер компонентов и бизнес-логика
│
├── modules/
│   ├── ui/             # UI модули (не компоненты!)
│   │   ├── chart-ui.js     # Chart.js логика
│   │   ├── table.js        # Логика таблицы
│   │   ├── themes.js       # Темы оформления
│   │   └── share.js        # Share логика
│   │
│   ├── utils/          # Утилиты
│   │   ├── format.js       # Форматирование
│   │   └── component-loader.js # Загрузчик компонентов ✅ NEW
│   │
│   ├── calc/           # Логика расчетов
│   │   ├── interest.js
│   │   ├── profitSharing.js
│   │   └── tax.js
│   │
│   ├── data/           # Данные
│   │   ├── banks.js
│   │   └── schema.js
│   │
│   ├── export/         # Экспорт
│   │   ├── csv.js
│   │   └── pdf.js
│   │
│   └── ui/components/  # КОМПОНЕНТЫ (новое!) ✅
│       ├── header/           # Компонент хедера
│       ├── calculator-form/  # Компонент формы
│       ├── export-panel/     # Компонент экспорта
│       └── table-chart/      # Компонент таблицы и графика
│
├── ARCHITECTURE.md     # Этот файл
├── CHANGELOG.md
├── VERSIONING.md
├── ROADMAP.md
└── AI_GUIDE.md
```

---

## 🧠 Роли файлов (дополнено с учетом компонентов)

### `script.js` — **Компонентный менеджер**
- Загружает компоненты через ComponentLoader
- Управляет жизненным циклом компонентов
- Обрабатывает события между компонентами
- Содержит бизнес-логику расчетов

### `modules/utils/component-loader.js` — **Загрузчик компонентов** ✅
- Динамически загружает HTML и JS компонентов
- Обрабатывает ошибки загрузки
- Предоставляет fallback UI

### `modules/ui/components/` — **Каталог компонентов** ✅
- Каждый компонент в своей папке
- Независимые HTML и JS файлы
- Четкие границы ответственности

---

## 🕰 История изменений архитектуры

### v0.5.0: Модульная архитектура
* UI логика вынесена в `/modules/ui/`
* `script.js` стал orchestration-слоем

### v0.6.0: Компонентная архитектура 🎉 ✅
* **Полный переход на компоненты**
* Создан `ComponentLoader` для динамической загрузки
* Разделен `index.html` на 4 компонента
* Улучшена поддерживаемость и масштабируемость

### v0.7.0: Business SaaS Minimalism Redesign 🎨 (планируется)
* Полный редизайн интерфейса
* Дальнейшая оптимизация CSS
* Улучшение UX/UI

---

## 🔮 Планы (`v0.7.x`)
* Оптимизация загрузки компонентов (lazy loading)
* Улучшение коммуникации между компонентами
* Добавление dark mode как компонента
* Micro-interactions для улучшения UX
* Дальнейшая декомпозиция крупных компонентов

---

## 🏁 Итог

Архитектура `v0.6.0+` — **компонентная, модульная и масштабируемая**.
Проект перешел от монолитной структуры к современной компонентной архитектуре,
где каждый UI элемент является независимым, переиспользуемым компонентом.
Это обеспечивает:
- ✅ Лучшую поддерживаемость
- ✅ Легкое тестирование
- ✅ Простоту добавления новых функций
- ✅ AI-friendly структуру
- ✅ Готовность к дальнейшему масштабированию
```
