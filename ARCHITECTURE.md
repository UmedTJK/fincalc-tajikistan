
🧱 1) **ARCHITECTURE.md**

**Создай файл и вставь 👇:**

```md
# 🧱 ARCHITECTURE.md — архитектура проекта

Проект: **FinCalc.TJ (v0.4.0)**  
Архитектурный стиль: *простая модульность → постепенный переход к ES Modules.*

---

## 📁 Структура проекта

```

/fincalc-tajikistan
│
├── index.html              # UI, структура интерфейса
├── style.css               # стили, темы, адаптив
 ├─ script.js  ← станет легче после создание отдельных interest.js banks.js share.js  chart.js 
 ├─ /modules
 │     ├─ interest.js     ← расчёты
 │     ├─ banks.js        ← банки
 │     ├─ share.js        ← шаринг
 │     ├─ chart.js        ← графики
│
├── DATA_BANKS.md           # формат и правила банковских данных
├── ROADMAP.md              # планы по годам
├── AI_GUIDE.md             # правила для ИИ
├── README.md               # описание проекта
│
└── LICENSE.md              # CC BY-NC

```

---

## 📌 Основные модули (логические)

| Модуль | Ответственность |
|--------|------------------|
| `calculateDeposit()` | центральная точка расчёта |
| `calculateWithCapitalization()` | проценты, налог, капитализация |
| `calculateAllCapitalizationScenarios()` | подготовка данных для графиков |
| `updateChart()` | визуализация данных |
| `exportToExcel()` / `exportToPDF()` | сохранение результатов |
| `initBanks()` | инициализация базы банков |

---

## 🎯 Целевое разделение (к v0.6.x)

Переход на структуру:

```

/modules
interest.js       # расчёты процентов и налогов
banks.js          # управление данными банков
charts.js         # диаграммы и сравнения
export/
pdf.js
excel.js

```
## Data Modules

### banks.js
Contains structured deposit data for supported banks in Tajikistan.
The module exports a single object `banksData` which is consumed by UI logic
(bank and deposit selectors) without any business logic inside.

Purpose:
- separation of data from UI logic
- easier maintenance and future updates
- preparation for JSON/API-based data sources


## 🔥 Принципы разработки

- Чистый JS (ES6+)
- Один экран → один поток действий
- Расчёты всегда отделены от DOM
- UI на русском, код на английском
- Минимум сторонних библиотек
- Чтобы код мог читать и ИИ, и студент

---

## 🛑 Что запрещено (пока)

- Фреймворки (React/Vue/Angular)
- TypeScript
- БД/Backend
- Смена налогового режима
- Многостраничный интерфейс

---

📌 **Архитектура утверждена:**  
Версия документа: **v1.0**
```

