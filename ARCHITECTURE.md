
# 📐 FinCalc.TJ — Architecture Overview

**Version:** v0.5.0
**Status:** Stable (post-UI refactor)

---

## 🎯 Цели архитектуры

Архитектура проекта направлена на:

* простоту поддержки
* прозрачность логики расчётов
* удобство масштабирования
* AI-friendly структуру (рефакторинг, анализ, автогенерация)

Ключевой принцип:

> **бизнес-логика отделена от UI и DOM**

---

## 🧱 Общая структура проекта

```txt
/
├── index.html          # Статическая HTML-разметка
├── style.css           # Глобальные стили
├── script.js           # Orchestration layer (без UI и расчётов)
│
├── modules/
│   ├── ui/             # Вся UI-логика (DOM, события, визуал)
│   │   ├── chart-ui.js     # Chart.js: инициализация и обновление графиков
│   │   ├── table.js        # Рендер таблицы расчётов
│   │   ├── themes.js       # Темы оформления
│   │   └── share.js        # Share / Web Share API / fallback
│   │
│   ├── utils/          # Чистые утилиты (без DOM)
│   │   └── format.js       # Форматирование чисел и дат
│   │
│   ├── interest.js     # Расчёт процентов (ядро)
│   ├── banks.js        # Данные банков
│   ├── charts.js       # Подготовка данных для графиков
│   ├── share.js        # (legacy / compatibility, не UI)
│   │
│   └── export/
│       ├── csv.js      # Экспорт CSV
│       └── pdf.js      # Экспорт PDF
│
├── ARCHITECTURE.md     # Этот файл
├── CHANGELOG.md
├── VERSIONING.md
├── ROADMAP.md
└── AI_GUIDE.md
```

---

## 🧠 Роли файлов

### `script.js` — **Orchestration layer**

* связывает модули между собой
* подписывается на события
* управляет порядком инициализации
* **НЕ содержит**:

  * UI-кода
  * DOM-манипуляций
  * логики форматирования
  * логики визуализации

---

### `/modules/ui/*` — **UI слой**

Отвечает **только** за:

* DOM
* обработчики событий
* визуализацию
* взаимодействие с пользователем

📌 UI-модули **НЕ выполняют расчёты**.

---

### `/modules/utils/*` — **утилиты**

* чистые функции
* без DOM
* без side-effects
* легко тестируются и переиспользуются

---

### Бизнес-логика (`interest.js`, `charts.js`)

* расчёты депозитов
* подготовка данных
* **НЕ знают ничего про UI**

---

## 🔒 Архитектурные правила (обязательные)

### ❌ Запрещено

* добавлять DOM или UI-код в `script.js`
* вызывать `document.*` внутри `utils`
* смешивать расчёты и визуал
* дублировать логику между модулями

### ✅ Разрешено

* передавать зависимости явно (например, `formatNumber`)
* расширять `modules/ui` новыми UI-модулями
* рефакторить `script.js`, сохраняя orchestration-роль

---

## 🕰 История изменений

### До `v0.5.0`

* монолитный `script.js`
* UI и логика перемешаны
* сложная поддержка

### С `v0.5.0`

* UI полностью вынесен в `/modules/ui`
* `script.js` стал orchestration-слоем
* инициализация стабилизирована
* расчётная логика не изменена

---

## 🔮 Планы (`v0.6.x`)

* централизованный `state`
* отказ от глобальных переменных
* URL-параметры / presets сценариев
* дальнейшее упрощение `script.js`

---

## 🏁 Итог

Архитектура `v0.5.0` — **стабильная, чистая и масштабируемая**.
Она является базой для всех последующих версий проекта.

