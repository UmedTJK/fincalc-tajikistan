
# 🧱 ARCHITECTURE.md — архитектура проекта

Проект: **FinCalc.TJ (v0.4.4)**  
Архитектурный стиль: *простая модульность → постепенный переход к ES Modules.*

---

## 📁 Текущая структура проекта (v0.4.4)

```

/fincalc-tajikistan
│
├── index.html              # UI, структура интерфейса
├── style.css               # стили, темы, адаптив
├── script.js               # основной контроллер UI и логики
│
├── /modules
│   ├── interest.js         # расчёт процентов и налога
│   ├── banks.js            # данные банков и вкладов
│   ├── charts.js           # подготовка данных для графиков
│   └── /export
│       └── csv.js          # экспорт расчётов в CSV (Excel)
│
├── assets/                 # иконки, изображения, статические ресурсы
│
├── DATA_BANKS.md           # формат и правила банковских данных
├── AI_GUIDE.md             # правила работы ИИ с проектом
├── ROADMAP.md              # планы развития
├── README.md               # описание проекта
├── CHANGELOG.md            # история изменений
├── VERSIONING.md           # правила версионирования
└── LICENSE.md              # CC BY-NC

```

---

## 📌 Основные логические блоки

| Функция / блок | Ответственность |
|---------------|------------------|
| `calculateDeposit()` | центральная точка пересчёта при изменении входных данных |
| `calculateWithCapitalization()` | помесячный расчёт: проценты, налог, капитализация |
| `calculateAllCapitalizationScenarios()` | расчёт и сравнение сценариев капитализации |
| `updateChart()` | отрисовка графика (UI-слой) |
| `exportToExcel()` | UI-обвязка экспорта CSV |
| `initBanks()` | инициализация выбора банка и депозита |

---

## 🧩 Реализованные модули

### interest.js
Модуль финансовых расчётов.

Назначение:
- расчёт начисленных процентов
- расчёт налога
- возврат чистого дохода

Экспорт:
- `calculateMonthlyInterest(amount, annualRate, taxRate)`

---

### banks.js
Contains structured deposit data for supported banks in Tajikistan.  
The module exports a single object `banksData` which is consumed by UI logic
(bank and deposit selectors) without any business logic inside.

Purpose:
- separation of data from UI logic
- easier maintenance and future updates
- preparation for JSON/API-based data sources

---

### charts.js
Prepares data series for deposit growth charts.

Responsibilities:
- формирование временных рядов
- подготовка данных для сравнения сценариев
- отсутствие зависимостей от DOM и Chart.js

---

### export/csv.js
Generates CSV report for Excel export.

Responsibilities:
- генерация CSV-контента
- отсутствие DOM-доступа
- чистая функция без побочных эффектов

---

## 🎯 Планируемое разделение (к v0.6.x)

> ⚠️ Ниже — **план развития**, не текущая реализация.

```

/modules
├── interest.js
├── banks.js
├── charts.js
├── share.js          # логика шаринга
└── /export
├── csv.js
└── pdf.js

```

---

## 🔥 Принципы разработки

- Чистый JavaScript (ES6+)
- Пошаговая и безопасная модульизация
- Расчёты всегда отделены от DOM
- UI — на русском языке
- Код — на английском языке
- Минимум зависимостей
- Код должен быть понятен:
  - студенту
  - ИИ
  - разработчику без контекста проекта

---

## 🛑 Что запрещено (на текущем этапе)

- Фреймворки (React / Vue / Angular)
- TypeScript
- Backend / базы данных
- Смена налоговой логики
- Многостраничный интерфейс
- Агрессивный рефактор без необходимости

---


## 🌍 Deployment

The project is deployed as a static site using GitHub Pages.
No backend or server-side logic is involved.

---

📌 **Архитектура утверждена**  
Версия документа: **v1.1**  
Актуальна для версии проекта: **v0.4.4**
```

---

## ✅ Статус

* 📦 Код — стабильный
* 📚 Документация — синхронизирована
* 🏷 Версия — `v0.4.4`
* 🧱 Архитектура — зафиксирована

**Этап v0.4.x официально закрыт.**
